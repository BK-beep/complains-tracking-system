spring:
  application:
    name: apigatewaykeycloak
  cloud:
    config:
      uri: http://config-service/8888
      enabled: true
      fail-fast: false
    discovery:
      enabled: true
  main:
    allow-bean-definition-overriding: true